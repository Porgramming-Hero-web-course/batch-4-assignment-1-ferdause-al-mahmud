{

    function sumArray(Arr: number[]): number {
        // using reduce function for suming the numbers
        return Arr.reduce((pre, curr) => pre + curr)
    }


    // const res = sumArray([1, 2, 3, 4, 5]);
    // console.log(res)

}