{
  "name": "your-project-name",
  "version": "1.0.0",
  "description": "A sample TypeScript project",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/problem-4.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  }
}
